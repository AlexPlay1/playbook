# Setup repositories
- name: Remove legacy Debian repository
  ansible.builtin.copy:
    content: ""
    dest: /etc/apt/sources.list

# Deprecation warn about ansible_ variables
- name: Add Debian repository
  ansible.builtin.template:
    src: templates/apt/debian.sources
    dest: /etc/apt/sources.list.d/debian.sources
    owner: root
    group: root
    mode: "0644"
  notify: Update apt cache

- name: Upgrade packages
  ansible.builtin.apt:
    update_cache: true
    upgrade: safe

# Install packages
- name: Install common utilities
  ansible.builtin.apt:
    update_cache: true
    name:
      - curl
      - wget
      - git
      - htop
      - ncdu
      - tmux
      - aptitude
      - rsync
      - iperf3
      - gnupg
      - lsb-release
      - unzip
      - xz-utils
      - zstd
      - logrotate
    state: present
